<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - BTS Indonesia</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; background: #0f172a; color: #e2e8f0; line-height: 1.6; }
    a { color: #60a5fa; text-decoration: none; }
    a:hover { color: #93c5fd; }
    .navbar { background: #1e293b; border-bottom: 1px solid #334155; padding: 0 2rem; display: flex; align-items: center; justify-content: space-between; height: 64px; }
    .navbar .logo { font-size: 1.3rem; font-weight: 700; color: #60a5fa; display: flex; align-items: center; gap: 8px; }
    .navbar .logo span { color: #f59e0b; }
    .nav-links { display: flex; gap: 1.5rem; align-items: center; }
    .nav-links a { color: #94a3b8; font-size: 0.9rem; padding: 0.5rem 0; transition: color 0.2s; }
    .nav-links a:hover { color: #f1f5f9; }
    .nav-links .btn-nav { background: #3b82f6; color: #fff; padding: 0.5rem 1.2rem; border-radius: 6px; font-weight: 600; }
    .nav-links .btn-nav:hover { background: #2563eb; }
    .container { max-width: 1200px; margin: 0 auto; padding: 2rem; }
    .btn { display: inline-block; padding: 0.6rem 1.5rem; border-radius: 8px; font-weight: 600; cursor: pointer; border: none; font-size: 0.9rem; transition: all 0.2s; }
    .btn-primary { background: #3b82f6; color: #fff; }
    .btn-primary:hover { background: #2563eb; }
    .btn-danger { background: #ef4444; color: #fff; }
    .btn-danger:hover { background: #dc2626; }
    .btn-success { background: #22c55e; color: #fff; }
    .btn-success:hover { background: #16a34a; }
    .btn-warning { background: #f59e0b; color: #000; }
    .btn-warning:hover { background: #d97706; }
    .btn-sm { padding: 0.3rem 0.8rem; font-size: 0.8rem; }
    .card { background: #1e293b; border: 1px solid #334155; border-radius: 12px; padding: 1.5rem; margin-bottom: 1rem; }
    .card h3 { color: #f1f5f9; margin-bottom: 0.5rem; }
    .form-group { margin-bottom: 1rem; }
    .form-group label { display: block; margin-bottom: 0.4rem; color: #94a3b8; font-size: 0.9rem; }
    .form-group input, .form-group select, .form-group textarea { width: 100%; padding: 0.7rem 1rem; background: #0f172a; border: 1px solid #334155; border-radius: 8px; color: #e2e8f0; font-size: 0.95rem; }
    .form-group input:focus, .form-group select:focus { outline: none; border-color: #3b82f6; }
    .alert { padding: 0.8rem 1.2rem; border-radius: 8px; margin-bottom: 1rem; font-size: 0.9rem; }
    .alert-success { background: #064e3b; color: #6ee7b7; border: 1px solid #065f46; }
    .alert-error { background: #450a0a; color: #fca5a5; border: 1px solid #7f1d1d; }
    .alert-info { background: #1e3a5f; color: #93c5fd; border: 1px solid #1e40af; }
    table { width: 100%; border-collapse: collapse; }
    table th, table td { padding: 0.7rem; text-align: left; border-bottom: 1px solid #334155; font-size: 0.85rem; }
    table th { background: #0f172a; color: #94a3b8; font-weight: 600; text-transform: uppercase; font-size: 0.75rem; }
    table tr:hover { background: #1a2744; }
    .badge { display: inline-block; padding: 0.2rem 0.6rem; border-radius: 20px; font-size: 0.75rem; font-weight: 600; }
    .badge-free { background: #374151; color: #9ca3af; }
    .badge-premium { background: #7c3aed22; color: #a78bfa; border: 1px solid #7c3aed; }
    .badge-vip { background: #f59e0b22; color: #fbbf24; border: 1px solid #f59e0b; }
    .badge-active { background: #065f46; color: #6ee7b7; }
    .badge-inactive { background: #7f1d1d; color: #fca5a5; }
    .grid-2 { display: grid; grid-template-columns: repeat(2, 1fr); gap: 1rem; }
    .grid-3 { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1rem; }
    .grid-4 { display: grid; grid-template-columns: repeat(4, 1fr); gap: 1rem; }
    .stat-card { background: #1e293b; border: 1px solid #334155; border-radius: 12px; padding: 1.5rem; text-align: center; }
    .stat-card .stat-value { font-size: 2rem; font-weight: 700; color: #60a5fa; }
    .stat-card .stat-label { color: #94a3b8; font-size: 0.85rem; margin-top: 0.3rem; }
    .code-block { background: #0f172a; border: 1px solid #334155; border-radius: 8px; padding: 1rem; font-family: 'Courier New', monospace; font-size: 0.85rem; overflow-x: auto; white-space: pre-wrap; word-break: break-all; color: #a5f3fc; }
    .pagination { display: flex; gap: 0.5rem; justify-content: center; margin-top: 1.5rem; }
    .pagination a, .pagination span { padding: 0.5rem 1rem; border-radius: 6px; background: #1e293b; border: 1px solid #334155; color: #94a3b8; }
    .pagination a:hover { background: #334155; color: #f1f5f9; }
    .pagination .active { background: #3b82f6; color: #fff; border-color: #3b82f6; }
    .flex { display: flex; }
    .flex-between { justify-content: space-between; }
    .flex-center { align-items: center; }
    .gap-1 { gap: 0.5rem; }
    .gap-2 { gap: 1rem; }
    .mt-1 { margin-top: 0.5rem; }
    .mt-2 { margin-top: 1rem; }
    .mb-1 { margin-bottom: 0.5rem; }
    .mb-2 { margin-bottom: 1rem; }
    .text-center { text-align: center; }
    .text-muted { color: #64748b; }
    .text-sm { font-size: 0.85rem; }
    @media (max-width: 768px) {
      .grid-2, .grid-3, .grid-4 { grid-template-columns: 1fr; }
      .navbar { padding: 0 1rem; }
      .nav-links { gap: 0.8rem; }
      .container { padding: 1rem; }
      table { font-size: 0.75rem; }
    }
  </style>
</head>
<body>
<nav class="navbar">
  <a href="/" class="logo">BTS<span>Indonesia</span> API</a>
  <div class="nav-links">
    <a href="/">Home</a>
    <a href="/towers">Towers</a>
    <a href="/docs">API Docs</a>
    <a href="/pricing">Pricing</a>
    <% if (typeof user !== 'undefined' && user && user.userId) { %>
      <a href="/msisdn-lookup">MSISDN Lookup</a>
      <a href="/dashboard">Dashboard</a>
      <% if (user.role === 'admin') { %>
        <a href="/admin" style="color: #f59e0b;">Admin</a>
      <% } %>
      <a href="/logout" class="btn-nav" style="background: #ef4444;">Logout</a>
    <% } else { %>
      <a href="/login" class="btn-nav">Login</a>
      <a href="/register">Register</a>
    <% } %>
  </div>
</nav>
